============
Installation
============

At the command line::

    $ pip install fuel-provision-poc

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv fuel-provision-poc
    $ pip install fuel-provision-poc
